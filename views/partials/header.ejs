<nav class="navbar">
    <div class="left">
      <a href="/">Home</a>
    </div>
  
    <div class="right">
      <% if (!user) { %>
        <a href="/login">Login</a>
        <a href="/signup">Signup</a>
      <% } else { %>
        <span>Hello, <%= user.username %></span>
        <a href="/logout">Logout</a>
      <% } %>
    </div>
  </nav>
  